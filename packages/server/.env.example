# Server
PORT=3001
AGENT_COUNT=10
TICK_INTERVAL=1000

# CORS Configuration
# For development: use "*" to allow all origins (reflects the request origin)
# For production: set to your frontend URL (e.g., https://your-app.railway.app)
# Multiple origins can be comma-separated (e.g., https://app1.com,https://app2.com)
CORS_ORIGIN=*

# AI Engine Configuration
# Set to "rule-based" for simple heuristic AI (no API key needed)
# Set to "llm" for DeepSeek-powered AI (requires DEEPSEEK_API_KEY)
AI_ENGINE=rule-based

# DeepSeek API Configuration (only needed if AI_ENGINE=llm)
# Get your API key from https://platform.deepseek.com/
DEEPSEEK_API_KEY=
# Model to use (default: deepseek-chat)
DEEPSEEK_MODEL=deepseek-chat
# Max concurrent LLM calls to avoid rate limits (default: 10)
DEEPSEEK_MAX_CONCURRENCY=10
