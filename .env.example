# AI Battle Royale - Environment Configuration
# Copy this file to .env and customize for your deployment

# =============================================================================
# Game Server Configuration
# =============================================================================

# Number of AI agents to spawn (default: 10)
AGENT_COUNT=10

# Milliseconds between game ticks (default: 1000)
TICK_INTERVAL=1000

# CORS origin for direct server access
# When behind Caddy proxy, use "*" since Caddy handles security
# For direct deployment, set to your frontend URL
CORS_ORIGIN=*

# =============================================================================
# AI Engine Configuration
# =============================================================================

# AI decision engine: "rule-based" or "llm"
# - rule-based: Simple heuristic AI (no API key required)
# - llm: DeepSeek-powered intelligent AI (requires API key)
AI_ENGINE=rule-based

# DeepSeek API Configuration (only if AI_ENGINE=llm)
# Get your key from: https://platform.deepseek.com/
# DEEPSEEK_API_KEY=sk-your-api-key-here
# DEEPSEEK_MODEL=deepseek-chat
# DEEPSEEK_MAX_CONCURRENCY=10

# =============================================================================
# Storage Configuration
# =============================================================================

# Thinking history storage: "memory" or "null"
# - memory: Store in-memory (lost on restart)
# - null: Disable storage (no thinking history)
THINKING_STORAGE=memory

# =============================================================================
# Frontend Configuration (packages/web/.env.local)
# =============================================================================

# WebSocket server URL
# - Development: http://localhost:3001
# - Production with Caddy: /api/game (relative path)
# NEXT_PUBLIC_SERVER_URL=/api/game
